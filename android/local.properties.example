sdk.dir=/path/to/Android/sdk

# Optional. Use this when running the Android app against a deployed API.
# Local emulator development falls back to http://10.0.2.2:3000.
EVENT_API_BASE_URL=https://your-deployed-web-app.example.com
